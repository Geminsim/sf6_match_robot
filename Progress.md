# 街霸6 机器人 (SF6 Match Robot) 新手配置指南

欢迎使用！如果你是第一次接触这种程序，不需要担心。这份指南会手把手教你如何从零开始，一步步把机器人成功运行起来并在你的频道里使用。

---

## 阶段 1：准备工作 (只需做一次)

### 1. 检查是否安装了 Python
这个机器人是用一种叫做 Python 的语言编写的，我们需要先让你的电脑能读懂它。
- 若你还不确定自己有没有装或者不懂这是什么，去浏览器搜索 [Python 官网](https://www.python.org/downloads/)，下载最新的 Windows 版本并正常安装即可。
- **重要提示**：安装 Python 时的第一个画面，底部有一个 **"Add Python to PATH"** （将 Python 添加到环境变量）的选项，**请务必把它打上勾！** 然后再点击 Install Now (立即安装)。

### 2. 运行一键配置脚本
为了省去复杂的敲代码指令，我们已为你准备好了自动安装包。
- 打开当前你下载/存放这个机器人的文件夹 (`sf6_match_robot`)
- 双击打开里面的 `scripts` 文件夹
- 找到 `setup.bat` (或者显示为配置名称的 `setup`) 这个文件，**双击运行它**。
- 会弹出一个黑色的窗口自动跑一些代码开始下载必要组件，不要关掉它。看到屏幕给出 "**Setup complete!**" （配置完成）并在等待你按任意键时，按键盘上的回车然后把黑窗口关掉就好啦！

---

## 阶段 2：去 Discord 召唤机器人

机器人要有“灵魂”，也就是要让 Discord 官方平台去承认它。

### 1. 领取机器人的“身份证” (Token)
- 用浏览器打开 [Discord 开发者中心页面](https://discord.com/developers/applications) 并登录你的 Discord 账号。
- 点击右上角的 **New Application** 按钮，给你的机器人起个名字并确认创建。
- 在左侧的菜单栏中，点击 **Bot** (机器人)。
- 点击页面中偏右一点名为 **Reset Token** (重置 Token) 的按钮。之后你会看到一长串乱码，那就是你专属机器人的“身份证证明”。
- **把那串乱码复制下来** (注意：这串乱码要像你的银行密码一样保密，绝不能泄露给其他人)。
- **极其重要的一步**：在同一个 Bot 页面往下滚动，找到 **Privileged Gateway Intents**（特权网关意图）板块，把里面的三个开关：`PRESENCE INTENT`、`SERVER MEMBERS INTENT` 和 `MESSAGE CONTENT INTENT` **全部打开（变成蓝色）**，然后点击底部弹出的绿色的 **Save Changes** 按钮保存！

### 2. 把机器人拉进你的服务器后台频道
- 还是在刚才那个开发者网页，左边菜单点击 **OAuth2** -> 然后展开点击 **URL Generator**
- 在 `SCOPES` 列表里，只需要勾选上 **bot** 和 **applications.commands**
- 下划到 `BOT PERMISSIONS` 列表里，勾选 **Administrator** (管理员权限) 保证机器人能管理频道
- 页面最底部会自动给你生成一个 **URL 链接**，复制那个链接。
- 把该链接粘贴到浏览器新标签页里并回车，然后选择让你想拉入机器人的 Discord 服务器，并点击同意即可。

---

## 阶段 3：告诉机器人它的密码密码并启动它

### 1. 配置机器人的密码本
- 回到机器人电脑文件夹的主目录下 （`sf6_match_robot` 文件夹内）
- 你会发现多了一个叫作 `.env` 的文件（刚才双击那个 Setup 图标以后自动生成的）。
- 用 **记事本** 打开这个 `.env` 文件。
- 找到这一行文字：`DISCORD_TOKEN=`
- 紧挨着等号`=`后面把你刚刚复制的那串长长的**乱码**直接粘贴上去。
*(比如：`DISCORD_TOKEN=Mzkw.......`)*
- 保存文件，然后关闭记事本！

### 2. 最终启动机器人！
万事具备，立刻让它开工干活：
- 你可以再次打开 `scripts` 文件夹，双击运行 `run.bat` (或者直接在外部主文件夹中运行这个命令脚本也可)。
- 一旦黑框框里跑出 `Logged in as XXX (id=xxx)` 这类提示，就说明你的机器人已经在线了！
- 回到 Discord 你所在的服务器里的任意频道，输入并发送斜杠命令 `/tournament create`，开始测试自动举办锦标赛吧！

> [!NOTE] 
> 以后每次想打开机器人的时候，只需要点击那个运行脚本即可。无需再重复前置安装或申请证明码了！
